<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="
		http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context
		http://www.springframework.org/schema/context/spring-context.xsd">
    <context:component-scan base-package="com.github.myproject"/>

    <context:property-placeholder location="classpath:db.properties"/>
    <!-- mybatis配置 -->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="configLocation" value="classpath:mybatis-config.xml"/>
        <property name="mapperLocations" value="classpath:com/github/myproject/mapper/*.xml"/>
        <property name="typeAliasesPackage" value="com.github.myproject.domain"/>
    </bean>
    <bean id="dataSource" destroy-method="close" class="org.apache.tomcat.jdbc.pool.DataSource">
        <property name="driverClassName" value="${db.driver}"/>
        <property name="url" value="${db.url}"/>
        <property name="username" value="${db.username}"/>
        <property name="password" value="${db.password}"/>

        <!-- Connection Pooling Info -->
        <property name="maxActive" value="${db.max-active}"/>
        <property name="maxIdle" value="${db.max-idle}"/>
        <property name="minIdle" value="${db.min-idle}"/>
        <property name="defaultAutoCommit" value="false" />
    </bean>
    <!-- 扫描basePackage下所有以@MyBatisRepository标识的 接口-->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="com.github.myproject"/>
        <property name="annotationClass" value="com.github.myproject.dao.Mapper"/>
    </bean>
</beans>